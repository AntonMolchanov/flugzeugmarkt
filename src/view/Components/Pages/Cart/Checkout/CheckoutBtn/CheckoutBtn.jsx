import React from 'react';
import '../checkout.scss'

function CheckoutBtn(props) {
    return (
        <div>
            <button type='submit' className="checkout-btn">Zur Kasse</button>
        </div>
    );
}

export default CheckoutBtn;